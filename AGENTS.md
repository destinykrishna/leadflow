# LeadFlow — Agent Instructions

## Project
LeadFlow

## Current Phase
Phase 8 — Backend Hardening & Performance (Complete — Prompts 1 & 2 Closed Out)

## Architecture
- **Monorepo Topology**:
  - `client/`: React 19, Vite, TanStack Query, Tailwind CSS.
  - `server/`: Express 5, Mongoose 9, TypeScript ESM, Zod.
  - `worker/`: BullMQ, Redis, asynchronous document verification & email processors.
  - `docs/`: Persistent architecture and database documentation (`docs/architecture.md`, `docs/database-design.md`, `docs/auth-security.md`, `docs/lead-ingestion.md`, `docs/pipeline-triggers.md`).
- **Server Layer Structure**:
  - `src/config/`: Environment configuration (`env.ts`) & database connection lifecycle (`database.ts`).
  - `src/models/`: Domain schemas and models with strict indexing, `brokerageId` scoping, `webhookSecret` integration, and `Session` rotation management (`Task`, `PipelineTrigger`, `TriggerExecution`, `EmailTemplate`).
  - `src/repositories/`: Lean data access layer enforcing tenant isolation (`withBrokerageScope`, `ScopedRepository`, `clientRepository`, `documentRepository`, `leadRepository`, `taskRepository`).
  - `src/services/`: Domain business logic orchestration (`token.service.ts`, `auth.service.ts`, `authorization.service.ts`, `lead-ingestion.service.ts`, `lead-pipeline.service.ts`, `trigger.service.ts`, `email.service.ts`).
  - `src/controllers/`: HTTP route controllers (`auth.controller.ts`, `brokerage.controller.ts`, `client.controller.ts`, `document.controller.ts`, `lead-ingestion.controller.ts`, `task.controller.ts`, `trigger.controller.ts`, `email-template.controller.ts`).
  - `src/middleware/`: Authentication context (`auth.middleware.ts`), RBAC guards (`rbac.middleware.ts`), webhook authentication (`webhook-auth.middleware.ts`), and centralized operational error handling (`error.middleware.ts`).
  - `src/routes/`: Express API routes (`auth.routes.ts`, `brokerage.routes.ts`, `client.routes.ts`, `document.routes.ts`, `lead.routes.ts`, `task.routes.ts`, `trigger.routes.ts`, `email-template.routes.ts`).
  - `src/validators/`: Zod runtime validation schemas (`auth.validators.ts`, `common.validators.ts`, `lead.validators.ts`, `task.validators.ts`, `trigger.validators.ts`, `email-template.validators.ts`).
  - `src/utils/`: Pino logger (`logger.ts`), custom errors (`errors.ts`), password/token hashing (`password.ts`), cookie helpers (`cookie.ts`), template rendering (`template.ts`), PII mask utility (`mask.ts`).
  - `src/queues/`: BullMQ queues and workers for document verification and email delivery (`document.queue.ts`, `document.worker.ts`, `email.queue.ts`, `email.worker.ts`).
  - `src/types/`: Domain TypeScript types and interfaces.
- **Testing Architecture**: Vitest test runner with isolated in-memory MongoDB via `mongodb-memory-server`.

## Technology
- **Runtime & Language**: Node.js (>=20.19.0), TypeScript 7 (NodeNext ESM).
- **Backend API**: Express 5.2.1, Mongoose 9.10.2, Zod 4.6.5, Pino 10.3.1, bcryptjs 3.0.3, jsonwebtoken 9.0.3, cookie-parser 1.4.7, express-rate-limit 8.7.0.
- **Database**: MongoDB 8+ (tested via `mongodb-memory-server` 11.3.0).
- **Background Jobs**: BullMQ 6.3.8, ioredis 6.0.0.
- **Real-Time**: Socket.IO 4.8.3.
- **Testing**: Vitest 5.0.1, Supertest 7.3.0.

## Domain Rules
- Use `Brokerage` as the business domain term (never `Tenant`).
- Four user roles supported: `PLATFORM_ADMIN`, `BROKERAGE_ADMIN`, `ADVISOR`, `CLIENT`.
- `PLATFORM_ADMIN` is a system-level role operating across the platform (`brokerageId` is optional/null).
- `BROKERAGE_ADMIN`, `ADVISOR`, and `CLIENT` are tenant-level roles and strictly require `brokerageId`.
- Tenant isolation key is `brokerageId: Types.ObjectId`.
- All tenant-scoped models must declare `brokerageId` (required, indexed) and `timestamps: true`.
- Compound unique constraints on tenant models must be scoped by `brokerageId` (e.g. `{ brokerageId: 1, email: 1 }`).
- Operational errors must inherit from `AppError` with explicit HTTP status codes and error codes.
- Do not over-engineer repositories; use them where they provide clear value for tenant isolation.

## Tenant Model
- Multi-tenancy follows the shared database, shared collection pattern with tenant discrimination.
- Every tenant query must strictly enforce `withBrokerageScope(brokerageId, filter)`.
- Cross-tenant data access attempts must throw `BrokerageIsolationError` (HTTP 403 / `BROKERAGE_ISOLATION_VIOLATION`).
- Socket.IO rooms are scoped per brokerage (`brokerage:<brokerageId>`); no global broadcasts of tenant data.
- Worker job payloads must carry `brokerageId` to verify tenant authorization during asynchronous processing.

## Architectural Decisions
- **Decision (Phase 1, Prompt 1)**: Configured NodeNext ESM modules across the server workspace.
- **Decision (Phase 1, Prompt 1)**: Integrated Mongoose 9 with `QueryFilter<T>` typing and created `withBrokerageScope` helper for safe, uniform query filtering.
- **Decision (Phase 1, Prompt 1)**: Standardized on `mongodb-memory-server` with global hooks in `tests/setup.ts` to guarantee test isolation without external database prerequisites.
- **Decision (Phase 1, Prompt 1)**: Established centralized error hierarchy (`AppError`, `BrokerageIsolationError`, `ValidationError`, `NotFoundError`).
- **Decision (Phase 1, Prompt 2)**: Implemented 8 core domain models (`Brokerage`, `User`, `Lead`, `Client`, `Document`, `Task`, `EmailTemplate`, `PipelineTrigger`) with full Mongoose 9 schemas, strict enum validations, and timestamps.
- **Decision (Phase 1, Prompt 2)**: Structured cross-brokerage duplicate tolerance by defining compound unique indexes on `{ brokerageId: 1, email: 1 }` (for `User`, `Lead`, `Client`) and `{ brokerageId: 1, slug: 1 }` (for `EmailTemplate`), enabling independent tenant operations without cross-tenant collisions.
- **Decision (Phase 1, Prompt 2)**: Embedded `Client.address` and `PipelineTrigger.actionConfig` as typed subdocuments (`_id: false`) to avoid unnecessary relational joins for tightly coupled 1:1 data.
- **Decision (Phase 1, Prompt 3)**: Aligned user roles with `assignment.md`: `PLATFORM_ADMIN`, `BROKERAGE_ADMIN`, `ADVISOR`, and `CLIENT`. Configured conditional validation on `User.brokerageId` so platform admins are not bound to a tenant, while enforcing strict tenant scoping on all other roles.
- **Decision (Phase 1, Prompt 3)**: Linked `Client.userId` to connect portal user accounts with client profiles, and expanded `Document.type` to include `PAYSLIP` for German mortgage expat workflows.
- **Decision (Phase 1, Prompt 3)**: Created test seed fixture (`server/tests/fixtures/seed.fixture.ts`) populating all 4 user roles and domain models for automated testing. Phase 1 complete.
- **Decision (Phase 2, Prompt 1)**: Implemented dual JWT + refresh session strategy. Access tokens are short-lived (15m) and self-contained with tenant context (`brokerageId`). Refresh tokens are long-lived (7d) and tracked via `Session` documents with SHA-256 hashed storage (`tokenHash`) and MongoDB TTL auto-expiry.
- **Decision (Phase 2, Prompt 1)**: Integrated RFC 6819 refresh token rotation and reuse detection using token family lineages (`family` UUID). Any replay attempt of an invalidated/replaced token triggers immediate revocation of all sessions in that family. Added unique `tokenId` (UUID) to payload to ensure distinct cryptographic signatures under sub-millisecond rotations.
- **Decision (Phase 2, Prompt 1)**: Handled multi-tenant login resolution for all 4 roles. `PLATFORM_ADMIN` authenticates globally with `brokerageId: null`. Tenant roles require an active user and active brokerage. Disambiguation across duplicate cross-brokerage emails supported via `brokerageSlug` or `brokerageId`.
- **Decision (Phase 2, Prompt 1)**: Configured secure HTTP-only cookies (`httpOnly: true`, `sameSite: 'lax'`, `secure: env.isProduction`) with body fallback for API clients. Added auth route rate limiting (`express-rate-limit`) with test environment bypass.
- **Decision (Phase 2, Prompt 1)**: Implemented `authenticate` middleware establishing strongly typed `req.user` context while enforcing database validation on user active status.
- **Decision (Phase 2, Prompt 2)**: Established centralized authorization primitives: `requireRoles` (HTTP 403 `FORBIDDEN` on unauthorized role), `requireSameBrokerage` (HTTP 403 `BROKERAGE_ISOLATION_VIOLATION` on URL param tenant mismatch, allowing `PLATFORM_ADMIN` cross-tenant access), and `requireActiveUser` (HTTP 401 on suspended/inactive account).
- **Decision (Phase 2, Prompt 2)**: Implemented generic `ScopedRepository<T, TDoc>` automatically scoping read and write operations to `{ brokerageId: user.brokerageId }` from trusted `req.user` server context, eliminating controller-level tenant query omissions.
- **Decision (Phase 2, Prompt 2)**: Enforced anti-IDOR protection with zero cross-tenant information leakage. Querying a guessed ID belonging to another brokerage or client returns `null` mapping to HTTP 404 (`NotFoundError`), completely concealing resource existence.
- **Decision (Phase 2, Prompt 2)**: Implemented `AuthorizationService` enforcing strict `CLIENT` role profile and document ownership (`userId === req.user.id` and `uploadedBy === req.user.id`), blocking lateral access to fellow expat clients within the same brokerage. Phase 2 complete.
- **Decision (Phase 2, Prompt 2 Audit)**: Completed focused audit of RBAC and tenant isolation. Hardened `requireSameBrokerage` against missing route parameters, added defensive ObjectId validation and service layer assertions to `BrokerageController.getBrokerageById`, guarded against falsy `uploadedBy` in `AuthorizationService`, and verified that the 7 skipped test suites are future phase stubs (`describe.todo`) with no skipped RBAC/tenant tests. Total 119 tests passing.
- **Decision (Phase 3, Prompt 1)**: Built Lead Ingestion Foundation supporting external webhooks (`POST /api/leads/webhook/:brokerageId` and `/api/leads/ingest/:brokerageId`). Implemented dual authentication via `x-webhook-secret` / Bearer token or HMAC SHA-256 (`x-signature-sha256`) with constant-time verification (`crypto.timingSafeEqual`). Normalization layer accepts standard JSON leads and external provider format (Typeform `form_response`), automatically extracting domain fields while discarding untrusted client `brokerageId` in request bodies. Scoped repository (`LeadRepository`) enforces deterministic duplicate detection on `{ brokerageId: 1, email: 1 }` and catches concurrent race conditions (MongoDB code 11000) to return existing leads idempotently (`isDuplicate: true`, HTTP 200) without crashing or creating duplicate documents. Total 150 tests passing.
- **Decision (Phase 3, Prompt 2)**: Hardened Lead Ingestion for reliability, concurrency, and privacy. Implemented anti-enumeration defense in `verifyWebhookAuth` returning uniform HTTP 401 `UnauthorizedError` to unauthenticated callers, preventing discovery of brokerage existence or active/suspended status. Implemented "already known" person detection in `LeadRepository.ingestLead` matching existing `Client` records within the brokerage, linking `existingClientId`, marking `knownAs: 'CLIENT'`, and inheriting current advisor assignments. Hardened MongoDB duplicate-key race condition absorption under high-volume burst ingestion. Masked PII (`maskEmail`) in structured ingestion logs. Phase 3 Lead Ingestion closed out with 159 passing tests.
- **Decision (Phase 3, Rate Limiting Fix)**: Replaced IP-based limiter with tenant-aware `brokerageIngestionLimiter` (1,000 req/min per verified brokerage) ordered strictly after `verifyWebhookAuth`. Keyed by `req.webhookBrokerage._id`, ensuring unauthenticated probes cannot consume brokerage quota and preventing noisy brokerages from throttling neighboring tenants sharing webhook egress IPs. Verified 500-request burst ingestion without throttling. Total 164 tests passing.
- **Decision (Phase 4, Prompt 1)**: Implemented Pipeline Foundation and State Machine (`NEW → CONTACTED → QUALIFIED → PROPOSAL → NEGOTIATION → WON / LOST`). Validated linear progression server-side while enforcing terminal immutability for `WON` and `LOST` (zero outgoing transitions) and allowing early drop-off to `LOST`. Integrated database-level optimistic concurrency using MongoDB atomic conditional updates matching `{ _id, brokerageId, status, __v }` and `$inc: { __v: 1 }` with `{ returnDocument: 'after' }`, returning HTTP 409 `ConflictError` on race conditions to prevent lost updates without external locks. Built authenticated pipeline endpoints (`GET /api/leads/pipeline`, `GET /api/leads`, `GET /api/leads/:id`, `PATCH /api/leads/:id/stage`) restricted to `PLATFORM_ADMIN`, `BROKERAGE_ADMIN`, and `ADVISOR` with strict tenant scoping and anti-IDOR HTTP 404 concealment. Added realtime event seam hook (`emitPipelineStageChanged`) ready for Socket.IO broadcasting in Phase 4 Prompt 2. Total 196 tests passing.
- **Decision (Phase 4, Prompt 2)**: Built Realtime Pipeline Updates with Socket.IO attached cleanly to HTTP server. Handshake authentication middleware (`socketAuthMiddleware`) validates JWT access tokens, active user standing, and active brokerage status (rejecting suspended tenants/users). Strictly server-controlled room assignments place advisors into isolated `brokerage:<brokerageId>` rooms, platform admins into `platform:admins`, and expat clients into private `client:<userId>` rooms—completely excluding `CLIENT` users from internal pipeline board events. Client-requested room joining or subscription events are blocked with 403 `FORBIDDEN`. Wired `emitPipelineStageChanged` to broadcast `pipeline:stage_changed` and `lead:stage_changed` only after successful database commits, broadcasting minimal state metadata with zero PII/credentials leakage. Total 216 tests passing.
- **Decision (Phase 5, Prompt 1)**: Implemented Client Conversion and Case Foundation. Defined server-side conversion eligibility matrix: leads in `QUALIFIED`, `PROPOSAL`, `NEGOTIATION`, or `WON` are convertible; `LOST`, `NEW`, and `CONTACTED` leads are rejected with 400 `ValidationError`. Enforced zero-infrastructure concurrency defense via atomic `Lead.findOneAndUpdate` claiming `{ convertedClientId: null }` and partial unique compound indexes on `Client` (`{ brokerageId: 1, leadId: 1 }` and `{ brokerageId: 1, userId: 1 }`) plus `{ brokerageId: 1, email: 1 }`. Integrated client portal identity provisioning with bcrypt hashing (`role: 'CLIENT'`, `status: 'ACTIVE'`) and automatic user linkage while defending against non-client account contamination. Preserved bidirectional `Lead ↔ Client` relationship (`client.leadId` & `lead.convertedClientId`) and original advisor assignments. Converted leads transition atomically to `WON`, broadcasting realtime pipeline events. Added IDOR-immune client case access API (`GET /api/clients/me` deriving identity strictly from verified session token; `GET /api/clients/:id` returning 404 on ID mismatch). Total 246 tests passing.
- **Decision (Phase 5, Prompt 2)**: Built Document Upload & Storage Foundation with ImageKit integration. Decoupled object storage behind `IStorageService` and `ImageKitStorageService` using `@imagekit/nodejs` v7 with deterministic server-controlled paths (`/leadflow/brokerage_${id}/clients/${clientId}`) and sanitized file names. Created Multer in-memory upload middleware enforcing 10MB limit and whitelisted MIME types (`PDF`, `JPEG`, `PNG`, `WEBP`, `TIFF`). Enforced strict tenant and case ownership: `CLIENT` users can only upload and list documents for their own verified case profile, returning 403 on client-to-client upload attempts. Guaranteed zero orphan files in ImageKit via defensive compensation rollback (`storageService.deleteFile`) if MongoDB persistence fails. Secured all document routes (`POST /api/documents/upload`, `GET /api/documents`, `GET /api/documents/:id`) against IDOR and credential leakage, keeping all ImageKit secrets server-side. Total 271 tests passing.
- **Decision (Phase 6, Prompt 1)**: Implemented BullMQ background document processing foundation. Upload endpoint enqueues verification jobs asynchronously to Redis queue (`document-processing`) with deterministic deduplication (`jobId: doc-verify-${documentId}`). Processor verifies persisted tenant boundaries (`withBrokerageScope`), rejecting cross-brokerage tampering via `UnrecoverableError`. Concurrency controls use atomic conditional updates (`{ _id, brokerageId, status: 'PENDING' }` to `PROCESSING`), preventing race conditions across distributed workers. Simulates realistic slow checks and supports transient retryable failures with exponential backoff (3 attempts) alongside terminal domain rejections (`status: 'REJECTED'`). The worker failure lifecycle handler guarantees documents are never left stuck in `PROCESSING` on exhausted retries. Realtime events (`document:status_changed`) broadcast committed state changes to tenant brokerage rooms (`brokerage:<brokerageId>`) and private client rooms (`client:<userId>`) with zero storage credential leakage. Total 283 tests passing.
- **Decision (Phase 6, Prompt 2)**: Hardened BullMQ background document processing for resilience, crash recovery, and zero data loss. Implemented eventual consistency enqueue isolation: if Redis is unavailable during HTTP upload, the document is safely persisted in MongoDB as PENDING without API request failure. Engineered a background reconciliation sweeper (`document-recovery.service.ts`) with MongoDB compound indexes on `{ status: 1, createdAt: 1 }` and `{ status: 1, updatedAt: 1 }` that automatically recovers unenqueued PENDING documents and stalled PROCESSING documents (resetting status to PENDING with explanatory audit notes and re-enqueueing). Wired non-blocking startup sweeps and periodic reconciliation intervals into both in-server workers and standalone worker processes (`worker/src/worker.ts`). Configured BullMQ worker stall detection (`lockDuration: 30s`, `stalledInterval: 15s`, `maxStalledCount: 2`). Verified duplicate delivery idempotency on terminal states (VERIFIED and REJECTED), multi-worker atomic state locking, anti-tampering tenant isolation (`UnrecoverableError`), and zero sensitive credential leakage in realtime broadcasts or Pino logs. Total 289 tests passing.
- **Decision (Phase 7, Prompt 1)**: Built the backend automation layer for pipeline triggers, advisor task generation, and asynchronous email delivery. Configured triggers execute non-blocking actions (`CREATE_TASK` and `SEND_EMAIL`) after committed stage transitions (`NEW` on ingestion, `PATCH /api/leads/:id/stage`, and `WON` on client conversion). Built atomic MongoDB idempotency tracking using `TriggerExecution` compound unique indexes (`{ brokerageId: 1, idempotencyKey: 1 }`) and `Task` partial filter indexes (`{ brokerageId: 1, idempotencyKey: 1 }` where `$type: 'string'`), preventing duplicate tasks or duplicate emails under concurrent stage updates. Created recursive template rendering (`renderTemplate`) supporting dot-notation nested placeholders (`{{lead.firstName}}`, `{{advisor.name}}`, `{{brokerage.name}}`, `{{customFields.*}}`). Decoupled email delivery into BullMQ queue (`email-delivery`) and worker (`email.worker.ts`) with bounded exponential backoff (3 attempts), terminal error differentiation (`UnrecoverableError`), and strict tenant boundary validation (`withBrokerageScope`). Pino logs mask PII emails (`maskEmail`) with zero credential exposure. Total 314 tests passing.
- **Decision (Phase 7, Prompt 2)**: Completed automation reliability and closeout review verifying all 7 invariants. Hardened template path interpolation in `template.ts` against prototype pollution and prototype traversal (`__proto__`, `constructor`, `prototype`, `toString`, `valueOf`), returning safe empty strings and restricting object access to verified own properties. Sanitized template context construction against sensitive credential leakage by stripping any keys matching `/password|token|secret|hash|apiKey|auth|creditCard|ssn/i` from custom fields and extra parameters. Verified post-commit trigger invocation across pipeline updates, lead ingestion, and client conversion. Confirmed BullMQ worker bounded retries (3 attempts, exponential backoff) and graceful multi-signal termination (`SIGINT`/`SIGTERM`) across workers, queues, and Redis connections. Added regression tests verifying prototype pollution defense and sensitive field suppression. Total 316 tests passing.
- **Decision (Phase 8, Prompt 1)**: Executed backend hardening and performance profiling pass. Reviewed and hardened MongoDB compound indexes across `Lead` (`{ brokerageId: 1, createdAt: -1 }` and `{ brokerageId: 1, assignedTo: 1, createdAt: -1 }`), `Task` (`{ brokerageId: 1, dueDate: 1, createdAt: -1 }`, `{ brokerageId: 1, status: 1, dueDate: 1 }`, and `{ brokerageId: 1, createdAt: -1 }`), `Document` (`{ brokerageId: 1, createdAt: -1 }` and `{ brokerageId: 1, status: 1, createdAt: -1 }`), and `Client` (`{ brokerageId: 1, createdAt: -1 }`), eliminating unindexed in-memory sorting on critical Kanban pipeline, task, and document listing queries. Enforced strict query pagination bounds (`limit` and `page`) on `TaskRepository.findTasks` and `DocumentService.listDocuments`, preventing unbounded memory allocation under high entity volumes. Benchmarked critical APIs using Autocannon: achieved 5,583 req/s on baseline health check, 234 req/s on full 200-lead 7-stage Kanban board retrieval (p50: 40ms, p99: 81ms), 442 req/s on indexed lead stage filtering (p50: 21ms), 356 req/s on task queries with overdue virtuals (p50: 27ms), and 1,735 to 2,184 req/s on webhook duplicate ingestion (p50: 4-10ms). Re-verified 500/min burst capacity with 2x headroom (1,000 req/min tenant rate limit) and noisy-neighbor tenant isolation. Added 9 focused integration tests in `tests/integration/hardening.test.ts`. Total 325 tests passing across 16 test files. Typecheck and production build clean.
- **Decision (Phase 8, Prompt 2)**: Completed final backend closeout and regression verification pass. Re-verified all 6 core backend invariants: 500/min lead ingestion capacity with 2x headroom (1,000 req/min per verified brokerage); MongoDB compound index coverage and query pagination memory safety; optimistic concurrency control (`__v` matching) and atomic idempotent duplicate absorption under high concurrency; strict tenant isolation (`withBrokerageScope`), anti-IDOR HTTP 404 concealment, active RBAC guards, and tenant-aware rate limiting; full backward compatibility across Phases 1–7; and zero sensitive data leakage in structured Pino logs or external API responses (`maskEmail` PII masking and credential suppression). Resolved Vitest in-memory database test runner concurrency conflicts by configuring `fileParallelism: false`. All 325 tests passing across 16 test files. Monorepo TypeScript check (`npm run typecheck`) and production build (`npm run build`) passing with zero errors. Phase 8 officially complete and backend declared production-ready.

## Rules
- Do not invent requirements.
- Inspect existing code before modifying it.
- Preserve established architecture.
- Do not implement future phases.
- Run tests/typecheck after changes.
- Document architectural decisions.